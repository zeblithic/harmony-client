{
  "identifier": "default",
  "description": "Default capabilities for the main window. `core:webview:allow-create-webview-window` is scoped to `main` only — the child `network-viz` window inherits the more restrictive `network-viz` capability (see network-viz.json) and cannot spawn further windows.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "dialog:default",
    "fs:allow-write-text-file",
    "updater:default",
    "deep-link:default",
    "os:default",
    "os:allow-hostname",
    "shell:allow-open"
  ]
}
