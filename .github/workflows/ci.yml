name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel older in-flight runs when a new commit lands on the same PR/branch.
# Saves CI minutes and gets the latest commit's feedback to the author faster.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  # ZEB-498: incremental compilation buys nothing on a once-through CI build,
  # and its artifacts bloat target/ — exactly what Swatinem/rust-cache tarballs
  # and saves. Disabling it shrinks each per-job cache (check/test/msrv) so the
  # three keys fit under GitHub's 10 GB/repo Actions-cache quota with headroom →
  # far less LRU eviction → PR runs reliably restore a warm dependency cache.
  CARGO_INCREMENTAL: '0'
  # ZEB-499: sccache → Cloudflare R2 backend config (not secrets). The actual
  # R2 credentials are injected per-Rust-job from secrets; RUSTC_WRAPPER is
  # gated on their presence (see each Rust job's "Enable sccache" step), so the
  # frontend job and fork PRs that lack the secret simply ignore these.
  SCCACHE_BUCKET: harmony-sccache
  SCCACHE_ENDPOINT: https://1ba234d340e59c6bba1e0fe90b7db8db.r2.cloudflarestorage.com
  SCCACHE_REGION: auto
  SCCACHE_S3_KEY_PREFIX: harmony-client
  # ZEB-499: keep the sccache server alive for the whole job. Its default 600s
  # idle timeout otherwise expires during rust-test's long *test-execution*
  # phase (no rustc calls after the binaries compile), so the server shuts down
  # and the final `sccache --show-stats` spins up a fresh one reporting 0 — the
  # compiles still cached to R2, but the telemetry was lost. 0 = never idle-out.
  SCCACHE_IDLE_TIMEOUT: '0'

# Least-privilege GITHUB_TOKEN: this workflow only checks out code and runs
# local validation commands — it never writes to the repo, opens PRs, posts
# comments, or pushes packages. Locking to read-only here means a compromised
# action or step can't escalate beyond what CI legitimately needs.
permissions:
  contents: read

# All third-party actions are pinned to full commit SHAs (not moving tags) so
# a compromised upstream or moved tag can't silently change what runs in CI.
# The trailing comment names the original tag for auditability — bump both
# together when refreshing.

jobs:
  # ZEB-273: split from the previous monolithic `rust` job. fmt + clippy run
  # here; tests run in parallel in `rust-test`. Wall-clock for the rust side
  # of CI drops from ~10 min (sequential fmt → clippy → test) to ~5 min
  # (longest-pole of clippy vs nextest), at the cost of ~80% more CI minutes.
  # Engineer wall-clock dominates over runner-minute cost.
  rust-check:
    name: Rust — fmt + clippy
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}
    runs-on: ubuntu-latest
    # 35 (post-ZEB-499 sccache→R2): clippy --all-targets warm runs land in
    # ~3-5 min off the R2 compile cache; 35 leaves headroom for a full-cache-miss
    # cold compile (e.g. a toolchain bump that invalidates R2). Walked back from
    # the 45 stopgap (ZEB-498).
    timeout-minutes: 35
    defaults:
      run:
        working-directory: src-tauri
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # Shared with release.yml via composite action — single source of
      # truth for the system libs tauri-runtime-wry's Linux backend pulls
      # in transitively (gdk-sys, webkit2gtk-sys, libsoup-sys, etc).
      - name: Install Tauri Linux deps
        uses: ./.github/actions/install-linux-tauri-deps

      # ZEB-498: mold is a drop-in ELF linker ~2-5x faster than the default
      # bfd/gold, and the gap widens on this workspace's ~119 statically-linked
      # integration test binaries — the relink no compile cache can skip (every
      # lib-touching PR relinks all of them). make-default:true symlinks
      # /usr/bin/ld -> mold so it needs no RUSTFLAGS, which keeps rust-cache's
      # key (and the warm dep cache) intact. Installed via the action's own
      # retrying wget — deliberately NOT apt, to stay off the mirror-flaky
      # install-linux-tauri-deps step (ZEB-498 P0 / PR #289).
      - name: Install mold linker (ZEB-498)
        # ZEB-800: mold is a pure speed optimization. The pinned action runs
        # shell:bash (-eo pipefail), so a failed wget|tar download aborts before
        # its `ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld)"` line, so a
        # failed install keeps the distro-default linker and yields a
        # functionally equivalent (slower) build. Do not let a transient network
        # blip red a required gate.
        continue-on-error: true
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444  # v1
        with:
          mold-version: '2.41.0'
          make-default: true

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9  # master
        with:
          toolchain: stable
          components: rustfmt, clippy

      # ZEB-499: install sccache (pinned, binary, off apt) — see docs/ci-sccache.md.
      - uses: taiki-e/install-action@ec28e287910af896fd98e04056d31fa68607e7ad  # v2.77.4
        with:
          tool: sccache

      # ZEB-499: enable sccache only when R2 is actually reachable. A startup
      # storage-check failure (bad secret, R2 down) is FATAL — sccache fails
      # every rustc invocation, it does NOT degrade to cache-miss the way
      # mid-build read/write errors do. So probe first: `sccache --start-server`
      # performs the init check (a read of .sccache_check); only if it succeeds
      # do we set RUSTC_WRAPPER. Otherwise (and for fork PRs with no secret) we
      # build uncached-but-green instead of taking CI red.
      - name: Enable sccache when R2 reachable (ZEB-499)
        env:
          R2_KEY: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
        run: |
          if [ -z "$R2_KEY" ]; then
            echo "sccache: no R2 credentials — building without sccache"
            exit 0
          fi
          if sccache --start-server; then
            echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
            echo "SCCACHE_ERROR_LOG=/tmp/sccache.log" >> "$GITHUB_ENV"
            echo "sccache: R2 reachable — enabled (prefix=$SCCACHE_S3_KEY_PREFIX)"
          else
            echo "::warning::sccache could not reach R2 (bad creds or outage) — building without sccache"
          fi

      # Per-job cache key so rust-check and rust-test don't trample each
      # other's compile artifacts. They share the deps cache (Cargo.lock-keyed)
      # but keep their own check-mode vs test-binary outputs.
      - uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae  # v2
        with:
          workspaces: src-tauri
          key: check
          # ZEB-499: sccache+R2 now owns compilation caching, so stop tarballing
          # target/ — this is what kills the 10 GB Actions-cache churn (ZEB-440).
          # rust-cache is kept solely to warm ~/.cargo (registry/index), which
          # sccache does not cache.
          cache-targets: false
          # ZEB-440: main-only cache saves — see the rust-test job's
          # rust-cache step for the full rationale.
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: cargo fmt --all -- --check
        run: cargo fmt --all -- --check

      # `--all-targets` is load-bearing: it builds tests/benches/examples,
      # not just the lib. Without it, integration test compile errors slip
      # through merge — exactly how ZEB-164's SidecarId migration broke
      # tests/content_index_integration.rs and tests/folder_primitive_integration.rs
      # but main stayed "green" for two days because contributors only
      # ran `cargo test --lib` locally.
      #
      # `--locked` ensures we test against the exact Cargo.lock dep graph
      # the repo committed. Without it, cargo can silently re-resolve
      # patched/yanked deps and CI would be testing a different graph than
      # contributors run locally. The MSRV job already uses --locked; the
      # main gate should match.
      # `--features test-fixtures` is required so the integration test
      # `tests/wire_format_channel_log_fixtures.rs` can compile — it
      # uses `encrypt_channel_packet_with_nonce`, a deterministic-nonce
      # variant of the production AEAD helper that is `#[cfg]`-gated to
      # `any(test, feature = "test-fixtures")` so it cannot be reached
      # from a release build (production code MUST use the random-nonce
      # `encrypt_channel_packet`; the deterministic variant is a
      # pin-test footgun if accidentally called). Integration tests
      # compile against the crate's public API and cannot see
      # `#[cfg(test)]`-only items, hence the feature gate.
      #
      # `--no-deps` keeps clippy focused on our own crate — third-party
      # dep lints are noise we can't fix.
      - name: cargo clippy --locked --all-targets
        run: cargo clippy --locked --all-targets --features test-fixtures --no-deps -- -D warnings

      - name: sccache stats (ZEB-499)
        if: always()
        run: |
          sccache --show-stats || echo "sccache not active this run"
          # ZEB-499: surface the sccache backend error log (auth/network/R2) so
          # failures like a bad R2 credential are visible, not just counted.
          if [ -n "${SCCACHE_ERROR_LOG:-}" ] && [ -s "${SCCACHE_ERROR_LOG}" ]; then
            echo "::group::sccache error log"; tail -n 200 "${SCCACHE_ERROR_LOG}"; echo "::endgroup::"
          fi

  # ZEB-676: sharded 3 ways with nextest's stable hash partitioning. By
  # 2026-07 the single rust-test job had grown to ~21-22 min and dominated CI
  # wall-clock 5-6× over every other job (~3.5-4 min). Measured split on a
  # zero-Rust-diff PR (run 29180367192): compile+link 3m47s at a 100% sccache
  # hit rate (841/841, R2 healthy) vs ~15.9 min executing 4,438 tests on the
  # 4-vCPU runner — the cost is TEST EXECUTION, which sccache cannot touch.
  # `--partition hash:k/3` divides only that execution phase: every shard
  # still compiles the full workspace (cheap, R2-cached) and runs a stable
  # ~third of the tests. Expected shard wall-clock ~10-13 min → CI total
  # (max of jobs) drops from ~22 to ~12 min. Hash partitioning is stable
  # per-test (no cross-run drift), so a failure always lands in the same
  # shard. When the corpus grows past shard ~15 min, bump the matrix + the
  # /3 denominators together. Standard runners are free on public repos, so
  # the 3× runner-minutes cost engineer wall-clock nothing (same trade as
  # the ZEB-273 check/test split).
  rust-test:
    name: Rust — test (nextest, shard ${{ matrix.shard }}/3)
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}
    runs-on: ubuntu-latest
    strategy:
      # One flaky/failing shard must not cancel the other two — a full
      # picture per run beats a fast abort (matches --no-fail-fast below).
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    # 35 (post-ZEB-499 sccache→R2): a warm SHARD lands in ~10-13 min (compile
    # ~4 min off R2 + ~1/3 of test execution); 35 leaves headroom for a
    # full-cache-miss cold compile (e.g. a toolchain bump that invalidates
    # R2). Walked back from the 45 stopgap (ZEB-498).
    timeout-minutes: 35
    defaults:
      run:
        working-directory: src-tauri
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # ZEB-440: this is the only job that LINKS — ~119 integration-test
      # binaries at ~90-108 MB each (line-tables-only debuginfo since ZEB-304,
      # statically-linked lib) ≈ 10-12 GB of executables per build generation,
      # before counting the
      # restored dep cache. The stock ubuntu-latest image leaves only
      # ~20 GB free, and June-2026 workspace growth (lib.rs +24%, test
      # files +19% in ten days) pushed peak usage past it: deterministic
      # ENOSPC at link, killing the job ~4.5 min in. Reclaim ~15-25 GB by
      # deleting preinstalled toolsets this repo never uses. Plain `rm -rf`
      # instead of a third-party free-disk action keeps the SHA-pinning
      # policy intact; rm -rf on absent paths is a no-op, so runner-image
      # layout changes degrade gracefully (telemetry below shows when the
      # reclaim stops paying). df output is the headroom trendline — read
      # it before adding the next 20 test binaries.
      - name: Reclaim runner disk (ZEB-440)
        run: |
          echo "=== disk before reclaim ==="
          df -h /
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/.ghcup \
            /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force > /dev/null 2>&1 || true
          echo "=== disk after reclaim ==="
          df -h /

      - name: Install Tauri Linux deps
        uses: ./.github/actions/install-linux-tauri-deps

      # ZEB-498: mold fast linker — see the rust-check job for the full
      # rationale. This is the only job that LINKS all ~119 integration test
      # binaries, so it benefits most. make-default:true → no RUSTFLAGS → the
      # rust-cache key stays intact.
      - name: Install mold linker (ZEB-498)
        # ZEB-800: mold is a pure speed optimization. The pinned action runs
        # shell:bash (-eo pipefail), so a failed wget|tar download aborts before
        # its `ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld)"` line, so a
        # failed install keeps the distro-default linker and yields a
        # functionally equivalent (slower) build. Do not let a transient network
        # blip red a required gate.
        continue-on-error: true
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444  # v1
        with:
          mold-version: '2.41.0'
          make-default: true

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9  # master
        with:
          toolchain: stable

      # ZEB-273: cargo-nextest is a faster test runner than `cargo test`.
      # Common 30-60% speedup on workspaces because it parallelizes test
      # execution at the binary level (cargo test parallelizes within a
      # binary but runs binaries sequentially by default). The
      # taiki-e/install-action installs a precompiled binary in ~5 sec
      # instead of `cargo install cargo-nextest` taking ~3 min.
      - uses: taiki-e/install-action@ec28e287910af896fd98e04056d31fa68607e7ad  # v2.77.4
        with:
          tool: cargo-nextest,sccache

      # ZEB-499: enable sccache only when R2 is actually reachable. A startup
      # storage-check failure (bad secret, R2 down) is FATAL — sccache fails
      # every rustc invocation, it does NOT degrade to cache-miss the way
      # mid-build read/write errors do. So probe first: `sccache --start-server`
      # performs the init check (a read of .sccache_check); only if it succeeds
      # do we set RUSTC_WRAPPER. Otherwise (and for fork PRs with no secret) we
      # build uncached-but-green instead of taking CI red.
      - name: Enable sccache when R2 reachable (ZEB-499)
        env:
          R2_KEY: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
        run: |
          if [ -z "$R2_KEY" ]; then
            echo "sccache: no R2 credentials — building without sccache"
            exit 0
          fi
          if sccache --start-server; then
            echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
            echo "SCCACHE_ERROR_LOG=/tmp/sccache.log" >> "$GITHUB_ENV"
            echo "sccache: R2 reachable — enabled (prefix=$SCCACHE_S3_KEY_PREFIX)"
          else
            echo "::warning::sccache could not reach R2 (bad creds or outage) — building without sccache"
          fi

      - uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae  # v2
        with:
          workspaces: src-tauri
          key: test
          # ZEB-499: sccache+R2 owns compilation caching now — stop tarballing
          # target/ (kills the 10 GB churn, ZEB-440). rust-cache warms ~/.cargo only.
          cache-targets: false
          # ZEB-440: only main updates the cache. PR-branch saves (~1.1 GB
          # compressed per round for this key) had pushed the repo past its
          # 10 GB Actions-cache quota into LRU eviction churn. PR runs
          # still RESTORE main's cache; they rebuild workspace crates on
          # top, which any lib-touching PR does anyway.
          save-if: ${{ github.ref == 'refs/heads/main' }}

      # nextest doesn't run doctests (`cargo test --doc` is the separate
      # doctest gate). At the time of writing, src-tauri has zero active
      # Rust doctests (the only fenced doc-block in src/follows.rs is
      # ```json, which doesn't compile as a doctest), so dropping cargo
      # test in favor of nextest is loss-free coverage-wise. If the
      # project gains Rust doctests later, add a `cargo test --doc` step
      # to this job before relying on nextest as the sole test gate.
      # ZEB-397: the depth-2+ nested-bundle round-trip
      # (`nested_bundle_tree_round_trip`) used to need a dedicated
      # `rust-test-large` job (HARMONY_LARGE_TESTS=1, 60-min timeout) because
      # it forced depth-2 by hashing a 36 GiB sparse file (~17 min) — the
      # per-PR CI long pole. It now forces the identical depth-2 path
      # (leaf count > MAX_BUNDLE_ENTRIES) with a tiny chunker config + a
      # ~140 MiB sparse fixture that runs in well under a second, so it is
      # un-gated and runs right here under --all-targets on every PR.
      # ZEB-676: each shard runs its stable hash-partition slice. The
      # partition filter applies at RUN time, after the full --all-targets
      # build — compile coverage (the ZEB-164 class of breakage) is still
      # complete in every shard.
      - name: cargo nextest run --workspace --all-targets (shard ${{ matrix.shard }}/3)
        run: cargo nextest run --locked --workspace --all-targets --features test-fixtures --no-fail-fast --partition 'hash:${{ matrix.shard }}/3'

      - name: sccache stats (ZEB-499)
        if: always()
        run: |
          sccache --show-stats || echo "sccache not active this run"
          # ZEB-499: surface the sccache backend error log (auth/network/R2) so
          # failures like a bad R2 credential are visible, not just counted.
          if [ -n "${SCCACHE_ERROR_LOG:-}" ] && [ -s "${SCCACHE_ERROR_LOG}" ]; then
            echo "::group::sccache error log"; tail -n 200 "${SCCACHE_ERROR_LOG}"; echo "::endgroup::"
          fi

      # ZEB-440 telemetry: residual usage after build + tests. When the
      # "Avail" number here trends below ~10 GB, the disk cliff is close
      # again — reach for the next lever (debuginfo trim / binary
      # consolidation) BEFORE it starts killing runs.
      - name: Disk headroom after tests
        if: always()
        run: df -h /

  # ZEB-676: single roll-up check over the shard matrix, keeping the familiar
  # "Rust — test (nextest)" status name stable for humans, bots, and any
  # future required-checks config — green iff all three shards passed.
  # `if: always()` so a shard failure still produces a (red) roll-up instead
  # of a skipped check that reads as "not run".
  rust-test-gate:
    name: Rust — test (nextest)
    needs: rust-test
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: All shards green?
        run: |
          result="${{ needs.rust-test.result }}"
          echo "shard matrix result: $result"
          [ "$result" = "success" ]

  # Validates `rust-version` declared in src-tauri/Cargo.toml. The `rust-check`
  # job above runs on stable for newer clippy lints + fastest iteration; this
  # job runs `cargo check --locked --all-targets` on the declared MSRV so
  # accidental use of post-MSRV Rust features (or a transitive dep that
  # raises its own MSRV) is caught at PR review, not at first compile on a
  # contributor's pinned-toolchain box. `check` (not `test`) keeps the job
  # fast — MSRV regressions surface as compile errors. `--locked` ensures we
  # test against the actual Cargo.lock dep graph, not whatever resolves
  # fresh.
  #
  # The MSRV value comes from a single source of truth — Cargo.toml's
  # `rust-version` — extracted at job time. This means a bump in Cargo.toml
  # automatically retargets the gate, no second edit required.
  msrv:
    name: MSRV — cargo check on declared rust-version
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_R2_SECRET_ACCESS_KEY }}
    runs-on: ubuntu-latest
    # 35 (post-ZEB-499 sccache→R2): cargo check --all-targets warm runs land in
    # ~3 min off the R2 cache; 35 leaves headroom for a full-cache-miss cold
    # compile. Walked back from the 45 stopgap (ZEB-498).
    timeout-minutes: 35
    defaults:
      run:
        working-directory: src-tauri
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Install Tauri Linux deps
        uses: ./.github/actions/install-linux-tauri-deps

      # ZEB-498: mold fast linker — see the rust-check job for the full
      # rationale. mold is invoked by the cc driver, so the MSRV toolchain
      # version is irrelevant. make-default:true → no RUSTFLAGS → rust-cache
      # key intact.
      - name: Install mold linker (ZEB-498)
        # ZEB-800: mold is a pure speed optimization. The pinned action runs
        # shell:bash (-eo pipefail), so a failed wget|tar download aborts before
        # its `ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld)"` line, so a
        # failed install keeps the distro-default linker and yields a
        # functionally equivalent (slower) build. Do not let a transient network
        # blip red a required gate.
        continue-on-error: true
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444  # v1
        with:
          mold-version: '2.41.0'
          make-default: true

      # Use cargo's own TOML parser via `cargo metadata` so single-quoted
      # strings, multi-line definitions, and any other valid TOML form all
      # parse correctly — a regex against Cargo.toml would not. `--no-deps`
      # skips dep resolution so this is a fast local-manifest read.
      # `cargo` is preinstalled on the GitHub-hosted ubuntu runner image,
      # so this step runs before any toolchain install.
      - name: Read MSRV from Cargo.toml
        id: msrv
        run: |
          set -uo pipefail
          msrv=$(cargo metadata --no-deps --format-version 1 --manifest-path Cargo.toml | jq -r '.packages[0].rust_version' || echo '')
          if [ -z "$msrv" ] || [ "$msrv" = "null" ]; then
            echo "::error::Cargo.toml has no [package].rust-version field; the MSRV gate has nothing to enforce" >&2
            exit 1
          fi
          echo "msrv=$msrv" >> "$GITHUB_OUTPUT"
          echo "Resolved MSRV from Cargo.toml: $msrv"

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9  # master
        with:
          toolchain: ${{ steps.msrv.outputs.msrv }}

      # ZEB-499: install sccache (pinned, binary, off apt) — see docs/ci-sccache.md.
      - uses: taiki-e/install-action@ec28e287910af896fd98e04056d31fa68607e7ad  # v2.77.4
        with:
          tool: sccache

      # ZEB-499: enable sccache only when R2 is actually reachable. A startup
      # storage-check failure (bad secret, R2 down) is FATAL — sccache fails
      # every rustc invocation, it does NOT degrade to cache-miss the way
      # mid-build read/write errors do. So probe first: `sccache --start-server`
      # performs the init check (a read of .sccache_check); only if it succeeds
      # do we set RUSTC_WRAPPER. Otherwise (and for fork PRs with no secret) we
      # build uncached-but-green instead of taking CI red.
      - name: Enable sccache when R2 reachable (ZEB-499)
        env:
          R2_KEY: ${{ secrets.SCCACHE_R2_ACCESS_KEY_ID }}
        run: |
          if [ -z "$R2_KEY" ]; then
            echo "sccache: no R2 credentials — building without sccache"
            exit 0
          fi
          if sccache --start-server; then
            echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
            echo "SCCACHE_ERROR_LOG=/tmp/sccache.log" >> "$GITHUB_ENV"
            echo "sccache: R2 reachable — enabled (prefix=$SCCACHE_S3_KEY_PREFIX)"
          else
            echo "::warning::sccache could not reach R2 (bad creds or outage) — building without sccache"
          fi

      - uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae  # v2
        with:
          workspaces: src-tauri
          key: msrv
          # ZEB-499: sccache+R2 owns compilation caching now — stop tarballing
          # target/ (kills the 10 GB churn, ZEB-440). rust-cache warms ~/.cargo only.
          cache-targets: false
          # ZEB-440: main-only cache saves — see the rust-test job's
          # rust-cache step for the full rationale.
          save-if: ${{ github.ref == 'refs/heads/main' }}

      # See the rust-check job's clippy step for why `--features
      # test-fixtures` is required to compile `--all-targets`.
      - name: cargo check --locked --all-targets
        run: cargo check --locked --all-targets --features test-fixtures

      - name: sccache stats (ZEB-499)
        if: always()
        run: |
          sccache --show-stats || echo "sccache not active this run"
          # ZEB-499: surface the sccache backend error log (auth/network/R2) so
          # failures like a bad R2 credential are visible, not just counted.
          if [ -n "${SCCACHE_ERROR_LOG:-}" ] && [ -s "${SCCACHE_ERROR_LOG}" ]; then
            echo "::group::sccache error log"; tail -n 200 "${SCCACHE_ERROR_LOG}"; echo "::endgroup::"
          fi

  frontend:
    name: Frontend — tsc, vitest
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: tsc --noEmit
        run: npx tsc --noEmit

      - name: vitest
        run: npx vitest run
